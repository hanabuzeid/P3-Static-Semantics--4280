 I assumed every test file ends with the extension ".ss21" 

to run p3: type "./statSem  [file]"

for example to run p3g1.ss21, type "./statSEM p2g1"

I have all the good tests saved already as p3g# (# being 1-2)

